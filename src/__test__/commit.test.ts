import { describe, test, expect, vi } from "vitest";
import { Commit } from "../class/commit";
describe("commitクラスのテスト", () => {
  const commit = new Commit("test commit", [], "testCommitID");
  test("初期化テスト", () => {
  })
});
